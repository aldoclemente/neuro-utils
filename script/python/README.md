```
	# Stores all the .nii files in path/to/dir as txt files.
	python3 nifti2txt.py --inputdir path/to/dir/
	
	# Stores all the .func.gii files in path/to/dir as txt files.
	# The txt files are available at path/to/dir/as_txt/
	python3 func.gii2txt.py --inputdir path/to/dir/
```
